"""fppe visual-navigation inference (skynet side).

Phase 1: offline harness that loads a GNM/ViNT/NoMaD policy and runs it on
image sequences to confirm it emits sane (distance, waypoint) outputs.
"""
